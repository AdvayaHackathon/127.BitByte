"use client"

import type React from "react"

import { LucideBrain } from "lucide-react"

export default function Brain(props: React.ComponentProps<typeof LucideBrain>) {
  return <LucideBrain {...props} />
}

